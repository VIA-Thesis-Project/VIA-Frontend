
  # Prototipo web AgroDesicions

  This is a code bundle for Prototipo web AgroDesicions. The original project is available at https://www.figma.com/design/WJjeYoKLo4kuDnxExS6r3I/Prototipo-web-AgroDesicions.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  